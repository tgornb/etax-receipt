{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Django Server",
            "type": "shell",
            "command": "${workspaceFolder}/.venv/bin/python manage.py runserver",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
